apply {
    from "${rootDir.path}/module.gradle"
}

android {
    defaultConfig {
        //如果是独立模块，则使用当前组件的包名
        if (!isRelease.toBoolean()) {
            applicationId "com.dandroid.module.login"
        }
    }
    //统一资源前缀，规范资源引用
    resourcePrefix "module_login"
}


dependencies {


}