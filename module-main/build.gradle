apply {
    from "${rootDir.path}/module.gradle"
}

android {
    defaultConfig {
        //如果是开发模块，则使用当前组件的包名
        if (!isRelease.toBoolean()) {
            applicationId "com.dandroid.module.main"
        }
    }

}


dependencies {

}